language: node_js
cache:
  yarn: true
  directories:
    - ~/.npm
notifications:
  email: true
node_js:
  - '8'
install: yarn
jobs:
  include:
    - stage: Release
      if: branch = master
      script:
        - yarn build
        - yarn verify-es5
        - yarn semantic-release
