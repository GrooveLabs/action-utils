language: node_js
cache:
  yarn: true
  directories:
    - ~/.npm
notifications:
  email: true
node_js:
  - '8'
install: yarn
jobs:
  include:
    - stage: build:prod
      if: branch = master
      script:
        - yarn build:prod
        - yarn travis-deploy-once --pro "yarn semantic-release"
